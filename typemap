TYPEMAP
git_repository* O_GIT_REPOSITORY

INPUT
O_GIT_REPOSITORY
    if( sv_isobject($arg) && (SvTYPE(SvRV($arg)) == SVt_PVMG) )
            $var = INT2PTR($type,SvIV((SV*)SvRV( $arg )));
    else{
            warn( \"${Package}::$func_name() -- $var is not a blessed SV reference\" );
            XSRETURN_UNDEF;
    }

OUTPUT
O_GIT_REPOSITORY
        sv_setref_pv( $arg, (char *)CLASS, (void*)$var );
